apiVersion: v1
kind: Service
metadata:
  name: elkia-gateway
  namespace: elkia
spec:
  ports:
    - name: elkia
      port: 4124
      targetPort: elkia
  selector:
    app.kubernetes.io/name: elkia-gateway
    app.kubernetes.io/instance: elkia-gateway
