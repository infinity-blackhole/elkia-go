apiVersion: v1
kind: Service
metadata:
  name: kratos-admin
  namespace: elkia
  labels:
    app: kratos
spec:
  ports:
    - port: 80
      targetPort: http-admin
      name: http
  selector:
    app: kratos
---
apiVersion: v1
kind: Service
metadata:
  name: kratos-public
  namespace: elkia
spec:
  ports:
    - port: 80
      targetPort: http-public
      name: http
  selector:
    app: kratos
---
apiVersion: v1
kind: Service
metadata:
  name: kratos-courier
  namespace: default
  labels:
    app: kratos-courier
spec:
  clusterIP: None
  ports:
    - port: 80
      targetPort: http-public
      name: http
  selector:
    app: kratos-courier
